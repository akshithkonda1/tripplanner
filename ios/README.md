# Trippy for iOS

SwiftUI app (iOS 17+). Generate the Xcode project on a Mac:

```bash
brew install xcodegen
xcodegen generate
open Trippy.xcodeproj
```

The app launches with sample Road, Flight, and Hybrid trips so you can walk the tabs without an API. Point `APIClient` at the deployed HTTP API when you have one.
