{
  "name": "Trippy",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "dynamodb-local",
      "command": "cd \"$HOME/dynamodb-local\" && exec java -Djava.library.path=./DynamoDBLocal_lib -jar DynamoDBLocal.jar -inMemory -port 8000"
    }
  ]
}
