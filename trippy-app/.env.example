# AI provider for Sam. "claude" (AWS Bedrock, default) or "grok" (xAI).
AI_PROVIDER=claude

# Claude runs on AWS Bedrock and uses AWS IAM credentials automatically -
# no API key needed.

# Grok (xAI) - required only when AI_PROVIDER=grok
GROK_API_KEY=your_xai_api_key_here
# Optional overrides (sensible defaults are used when unset):
# GROK_MODEL=grok-4
# GROK_PLANNING_MODEL=grok-4
# GROK_API_URL=https://api.x.ai/v1/chat/completions

# Weather - FREE, no API key required.
#   * iOS app: uses Apple WeatherKit natively (free with an Apple Developer account).
#   * Backend: uses Open-Meteo (free, keyless) for trip planning.
# Options: "open-meteo" (default) or "mock" (offline development).
WEATHER_PROVIDER=open-meteo

# GraphHopper (optional, for server-side routing; app falls back to mock)
GRAPHHOPPER_API_KEY=your_graphhopper_api_key_here

# Booking Affiliate IDs
BOOKING_AFFILIATE_ID=your_booking_affiliate_id
EXPEDIA_AFFILIATE_ID=your_expedia_affiliate_id
VIATOR_PARTNER_ID=your_viator_partner_id

# AWS (for deployment)
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=your_aws_account_id

# API Endpoints (set after deployment)
HTTP_API_URL=https://your-api.execute-api.us-east-1.amazonaws.com
WS_API_URL=wss://your-ws-api.execute-api.us-east-1.amazonaws.com/production
